
--create table offer (offer_id bigint not null, activation_status integer, created_on timestamp, currency varchar(255), 
--description varchar(255), expired_on timestamp, offer_name varchar(255), validity integer not null, primary key (offer_id))
